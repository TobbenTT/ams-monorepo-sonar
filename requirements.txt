# OCP Maintenance AI MVP — Dependencies (pinned)
pydantic==2.12.5
pytest==9.0.3
python-dateutil==2.9.0.post0
# Sentry (free tier) — opcional, se activa si SENTRY_DSN_BACKEND está seteado
sentry-sdk[fastapi]==2.19.0

# Multi-Agent System (Claude Agent SDK)
anthropic==0.83.0
pytest-asyncio>=0.23.0

# Phase 2: Application Layer
fastapi==0.133.1
uvicorn[standard]==0.41.0
sqlalchemy==2.0.47
python-dotenv==1.2.2  # CVE-2026-28684 fix: symlink TOCTOU en .env rewrite
httpx==0.28.1
plotly==6.5.2
openpyxl==3.1.5
xlrd==2.0.1  # legacy .xls (Jigsaw 2010 imports)

# OR System — document generation (Word, PowerPoint)
python-docx==1.1.2
python-pptx==1.0.2

# File uploads (UploadFile / Form)
python-multipart==0.0.26  # CVE-2026-40347 fix: DoS en multipart parsing

# Production server
gunicorn==25.1.0

# Authentication
PyJWT[crypto]==2.12.0
bcrypt==4.2.1
psycopg2-binary==2.9.11
pyotp==2.9.0
qrcode==7.4.2

# Phase 2 RAG (#33 RCM, #34 Shift Handover, #35 Post-Maint Learning, #40 KB Curator)
# torch CPU-only via Dockerfile (--extra-index-url https://download.pytorch.org/whl/cpu)
sentence-transformers==3.3.1
lancedb==0.16.0
pyarrow==18.1.0
