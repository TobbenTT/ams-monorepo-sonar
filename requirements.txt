# ⚠️  DEPRECATED — Las deps ahora viven en pyproject.toml por servicio:
#   apps/core/pyproject.toml        ← backend FastAPI
#   apps/sap_mock/pyproject.toml    ← mock OData
#   packages/tools/pyproject.toml   ← engines + schemas
#   packages/agents/pyproject.toml  ← AI agents
#   packages/skills/pyproject.toml  ← skill defs
#
# El Dockerfile principal instala las deps de ams-core directamente desde
# su pyproject. Este archivo queda como fallback para herramientas viejas
# (`pip install -r requirements.txt`) hasta que CI / scripts migren.
#
# Para instalar todo local en desarrollo:
#   pip install -e packages/tools -e packages/agents -e packages/skills
#   pip install -e "apps/core[test]"
#
# Las siguientes líneas reflejan apps/core/pyproject.toml [project.dependencies]
# (sin las internal workspace packages, que se editable-install por separado).

fastapi==0.136.1
uvicorn[standard]==0.46.0
gunicorn==25.1.0
python-multipart==0.0.28
sqlalchemy==2.0.49
pydantic==2.13.4
python-dotenv==1.2.2
python-dateutil==2.9.0.post0
httpx==0.28.1
PyJWT[crypto]==2.12.1
bcrypt==4.2.1
pyotp==2.9.0
qrcode==7.4.2
anthropic==0.102.0
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
xlrd==2.0.2
sentry-sdk[fastapi]==2.60.0
pytest==9.0.3
pytest-asyncio>=0.23.0
PyYAML>=6.0
